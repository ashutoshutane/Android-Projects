<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Buttons" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor" value="@color/my_light_primary" />
        <item name="android:navigationBarColor" value="@color/my_light_primary" />
    </style>


    <style name="Theme.Buttons" parent="Base.Theme.Buttons" />
</resources>